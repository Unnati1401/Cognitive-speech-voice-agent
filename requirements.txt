# Runtime deps for the Hugging Face Space — EXTRAS ONLY.
# Do NOT pin gradio / gradio_client / starlette / fastapi / pydantic / huggingface_hub
# here. The Gradio SDK Space already installs a coherent set of those; pinning them
# is what caused the HfFolder / bool-schema / TemplateResponse crashes.
spaces          # required for ZeroGPU (@spaces.GPU)
numpy>=1.26
pandas>=2.2
scipy>=1.13
pyyaml>=6.0
torch>=2.3
torchaudio>=2.3
faster-whisper>=1.0
pyannote.audio>=3.1
soundfile>=0.12
librosa>=0.10
opensmile>=2.5
spacy>=3.7
en_core_web_sm @ https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.7.1/en_core_web_sm-3.7.1-py3-none-any.whl
sentence-transformers>=3.0
transformers>=4.44
scikit-learn>=1.5
xgboost>=2.1
openai>=1.40